<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daejeonpeople">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Splash" />
        <activity android:name=".activities.SignUp"/>
        <activity android:name=".activities.Email_Certified" />
        <activity android:name=".activities.Chatting"/>
        <activity android:name=".activities.AddressBook" android:theme="@style/CustomActionBarTheme"/>
        <activity android:name=".activities.SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Main"/>
        <activity android:name=".activities.Category" />
        <activity android:name=".activities.EntryID" />
        <activity android:name=".activities.EntryPW" />
        <activity android:name=".activities.FindID" />
        <activity android:name=".activities.FindPW" />
        <activity android:name=".activities.Search" />
    </application>

</manifest>